<div class="media-heading">
    <% if notification.tag == 'follow' %>
        <%= link_to notification.actor.username, main_app.user_path(notification.actor) %>
        has followed you:
        <%= link_to notification.user.name, main_app.user_path(notification.user) %>
    <% end %>
</div>