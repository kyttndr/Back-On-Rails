<div class="media-heading">
    <% if notification.actor && notification.user %>

        <% if notification.tag == 'new_follower' %>
            <%= link_to notification.actor.username, main_app.user_path(notification.actor) %>
            has started following you!
        <% end %>

    <% end %>
</div>
